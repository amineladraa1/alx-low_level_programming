#include "main.h"

/**
 * _isalpha - Entry point
 *
 * Description: 'the program's description'
 *
 * @c : the character to check
 *
 * Return:  0 if it's alphabet 1 if it's not
 */

int _isalpha(int c)
{
	if ((c >= 65 && c <= 90) || (c >= 97 && c <= 122)
		      return (1);	
	return (0);
}
